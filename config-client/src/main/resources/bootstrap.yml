
spring:
  cloud:
    config:
      uri: http://localhost:9000/
      profile: dev
      label: master
      discovery:
        enabled: true
        service-id: config-server
  application:
    name: config-client